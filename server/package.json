{
  "name": "wes-portfolio-server",
  "version": "1.0.0",
  "description": "Mail sending server",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"cd ../ && npm start\"",
    "server":"nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "1wes",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemailer": "^6.9.3"
  }
}
